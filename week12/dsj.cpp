#include <bits/stdc++.h>
using namespace std;

//dsj set find когда мы используем find(V) то пишем так что когда ищем парента вершины то его парент это множество в котором он находится


int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
    return 0;
}